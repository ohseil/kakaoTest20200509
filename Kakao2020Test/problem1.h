#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <algorithm>

using namespace std;

namespace problem1 {

	int calculateDistance(pair<int, int> a, pair<int, int> b);
	string solution(vector<int> numbers, string hand);

	void execute();
}